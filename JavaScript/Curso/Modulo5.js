//Crear array
let frutas = ["Manzanas", "Peras", "Bananas"];
let numeros = new Array(1,2,3,4,5);

//Acceder a los elementos
console.log(frutas[2]);

numeros[1] = 1;
console.log(numeros);

//Recorrer el array
for (let i = 0; i < numeros.length; i++) {
    console.log(numeros[i]);
}

//longitud
console.log(frutas.length);


//PILAS | FIFO

unshift() //Agrega al inicio.
shift() //Elimina el primer 

indexOf() // Devuelve el índice de un elemento
splice() // Añade o elimina elementos de un array.
slice() //Extrae una sección del array sin modificar el array original.

push() // Agrega al final
pop() // Elimina el último 



